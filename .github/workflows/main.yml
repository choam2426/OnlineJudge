name: 🤖 AI Algorithm Mentor

on:
  push:
    branches: [ "main" ]

jobs:
  ai-review:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: call AI Algorithm Mentor
        uses: choam2426/AI-Algorithm-Mentor@feature/#8
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LLM_PROVIDER: openai              # openai, google, anthropic
          MODEL_NAME: gpt-5.1                 # 모델명 (선택사항)
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          REVIEW_LANGUAGE: korean           # korean, english, etc..
